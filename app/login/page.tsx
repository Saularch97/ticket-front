import { Form } from "@/components/ui/form";
import { TicketForm } from "@/components/ui/ticket_form";

 export default function LoginPage() {
  return (
    <main className="flex items-center justify-center md:h-screen">
        <TicketForm></TicketForm>
    </main>
  );
}